<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Board">
    <insert id="save" parameterType="board">
        insert into board_table(boardWriter, boardPass, boardTitle, boardContents)
        values (#{boardWriter}, #{boardPass}, #{boardTitle}, #{boardContents})
    </insert>

    <select id="findFile" parameterType="Long" resultType="boardFile">
        select * from board_file_table where boardId=#{boardId}
    </select>

    <select id="findAll" resultType="board">
        select * from board_table ;
    </select>

<!--    <select id="findAll2" resultType="board">-->
<!--        select * from board_table order by id desc limit 3 offset 3;-->
<!--    </select>-->

    <select id="findById" parameterType="Long" resultType="board">
        select *
        from board_table
        where id=#{id}
    </select>

    <update id="upHits" parameterType="Long" >
        update board_table set board_hits=board_hits+1 where id=#{id};
    </update>

    <update id="update" parameterType="board">
        update board_table
        set boardTitle=#{boardTitle},
            boardContents=#{boardContents}
        where id = #{id};
    </update>

    <delete id="delete" parameterType="Long">
        delete from board_table where id=#{id}
    </delete>

    <select id="findOne" parameterType="board" resultType="board">
        select *
        from board_table
        where boardTitle = #{boardTitle}
    </select>
</mapper>














