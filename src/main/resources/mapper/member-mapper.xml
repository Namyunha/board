<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member">
    <insert id="save" parameterType="member">
        insert into member_table2(memberId, memberPassword, memberName, memberBirth, gender, memberEmail, memberMobile)
        values (#{memberId}, #{memberPassword}, #{memberName}, #{memberBirth}, #{gender}, #{memberEmail},
                #{memberMobile})
    </insert>

    <select id="login" resultType="member" parameterType="member">
        select * from member_table2 where memberId=#{memberId} and memberPassword=#{memberPassword}
    </select>

    <select id="findAll" resultType="member">
        select * from member_table2
    </select>

    <select id="findById" resultType="member" parameterType="String">
        select * from member_table2 where memberId=#{memberId}
    </select>
</mapper>














